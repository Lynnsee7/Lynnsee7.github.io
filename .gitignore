_site/
.jekyll-cache/
.jekyll-metadata
push.sh
.DS_Store

# Anchored to the repo root on purpose: an unanchored "vendor/" also matches
# _sass/vendor/, which holds theme stylesheets that must be committed.
/vendor/
/.bundle/
